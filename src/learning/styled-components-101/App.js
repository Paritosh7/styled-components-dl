import Quote from "./Quote";

const Wrapper = () => {
  return (
    <Quote
      by="Bill Gates (Allegdly)"
      source="https://quoteinvestigation.com/2011/09/08/640k-enough/"
    >
      640kb of memory ought to be enough for anyone
    </Quote>
  );
};

export default Wrapper;
